# P2-07 secrets & runtime artifacts —— 严禁进 repo。配置用 .env.example 作模板。
.env
.env.*
!.env.example
*.log
logs/
server.log
/target
Cargo.lock
config.toml

# macOS
.DS_Store

/storage

# RS256 unified token keys (spec TOKEN_UNIFICATION_SPEC v1.3 §4.4 / §11.1)
# 私钥永远不进 repo。仅提交 keys/README.md 说明生成命令。
/.local/
/keys/*
!/keys/README.md
*.pem
*.key

# 服务端长期 TLS 证书与私钥（gen-server-tls.sh 生成，绝不进 Git）
/certs/
