-- 027: 删掉「转发来源」表
--
-- 产品定义里单条转发**不展示来源**：不关心转发自哪里、原作者是谁（微信式，
-- 不是 Telegram 式）。副本就是目标会话里一条普通消息，只是复用了同一份附件。
--
-- 这张表是按 Telegram 的 `messageFwdHeader` 建的，从未有产品需求支撑它。
-- 它的存在还派生出一整条「来源必须进 canonical 投影 + 三条投递链」的前置要求——
-- 那条要求服务的功能，我们并不需要。留着它就是留着这份前置。
--
-- 表里没有生产数据（写入路径随本次改动一并删除，转发路由从未注册）。

DROP TABLE IF EXISTS privchat_message_forward_origin;
